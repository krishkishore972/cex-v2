import { redisManager } from "@repo/redis";








export const waitForOrderResponse = async (clientId: string): Promise<{status:"success"|"error",data:any}> => {
    return new Promise((resolve) => {

    }
}